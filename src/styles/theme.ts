export type Theme = {
  palette: {
    primary: string;
    gray: string;
    lightGray: string;
  };
};

export const theme: Theme = {
  palette: {
    primary: '#2799fb',
    gray: 'gray',
    lightGray: '#DADADA',
  },
};
