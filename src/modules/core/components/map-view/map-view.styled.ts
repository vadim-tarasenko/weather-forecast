import styled from 'styled-components/native';
import RNMapView from 'react-native-maps';

export const Map = styled(RNMapView)`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`;
